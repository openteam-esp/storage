system:
  address: 'http://localhost:3000'

production:
  s3:
    access_key_id:      key_id
    secret_access_key:  secret_access_key

  hoptoad:
    key:                key
    host:               host

